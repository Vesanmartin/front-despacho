version: '3.8'

services:
  frontend:
    build: .
    image: verosanmartin/innovatech-frontend:latest
    ports:
      - "80:80"
    networks:
      - frontend-net

networks:
  frontend-net:
    driver: bridge
